webtest2/
node_modules/
.next/
.env
.env.local
.env*.local
logs/
.DS_Store

# runtime state (auto-seeded on first boot; contains the hashed admin
# password + session secret once the app has run — never commit it)
data/
*.tsbuildinfo
next-env.d.ts

# editor live-preview drafts never get committed
content/docs/_preview.mdx*
